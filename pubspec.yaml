name: flutter_structure_maker
description: A simple CLI tool to generate folder structure for Flutter projects automatically.
version: 1.0.2
homepage: https://pub.dev/packages/flutter_structure_maker
license: MIT

# repository: https://github.com/my_org/my_repo

executables:
  flutter_structure_maker: flutter_structure_maker

environment:
  sdk: ">=2.12.0 <4.0.0"
  flutter: ">=2.0.0"
# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  args: ^2.5.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
